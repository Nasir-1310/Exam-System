# Exam System Backend

## To run the project
```bash
chmod +x run.sh
./run.sh
```

## Commands for migrations
```bash
chmod +x migration.sh
./migration.sh --init
./migration.sh create "migration message"
./migration.sh run
```